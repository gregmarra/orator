.build/
Orator.app/
*.xcodeproj
.DS_Store
.signing-identity
*.p12
*.pem

# Local scratch (not committed)
todo.md

# Rendered media: only the README hero GIF is tracked; PNG stills + mp4 are
# regenerable via OratorReel.
assets/*
!assets/reel.gif
